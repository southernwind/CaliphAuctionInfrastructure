## Temporary HTTP-only config (pre-certificate) for all domains

# Apex -> redirect to www when cert phase completes (still keep challenge)
server {
    listen 80;
    server_name caliphauction.com;
    location /.well-known/acme-challenge/ {
        root {{ letsencrypt_webroot }};
    }
}

# www domain temporary (redirect to https www once cert ready)
server {
    listen 80;
    server_name www.caliphauction.com;
    location /.well-known/acme-challenge/ {
        root {{ letsencrypt_webroot }};
    }
}

# api domain temporary
server {
    listen 80;
    server_name api.caliphauction.com;
    location /.well-known/acme-challenge/ {
        root {{ letsencrypt_webroot }};
    }
}

# cdn domain temporary
server {
    listen 80;
    server_name cdn.caliphauction.com;
    location /.well-known/acme-challenge/ {
        root {{ letsencrypt_webroot }};
    }
}
